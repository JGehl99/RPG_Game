package com.mygdx.rpg_game.entity.player

/**
 * Enum class to store Player states.
 *
 * @author Joshua Gehl
 */
enum class PlayerState {
    STATE_IDLE,
    STATE_DOWN,
    STATE_LEFT,
    STATE_RIGHT,
    STATE_UP
}